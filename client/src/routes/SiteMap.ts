import { type SiteMap } from "@/interfaces/routes/SiteMap"

const siteMap: SiteMap = {
  ACCUEIL: {
    path: "/app/home",
    label: "Accueil"
  },
  REQUEST: {
    path: "/app/request",
    label: "Request"
  }
}

export default siteMap
